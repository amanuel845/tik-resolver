[project]
name = "tik-resolver"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "Flask==3.0.3",
    "yt-dlp",
]

[[tool.vercel.entrypoints]]
path = "api/resolve"
entrypoint = "app"

[[tool.vercel.entrypoints]]
path = "api/download"
entrypoint = "app"
